# Workflow Engine (`workflow-engine`)

Durable execution engine responsible for orchestrating multi-step operational state machines across physical and digital entities (e.g., quality hold -> technician dispatch -> lot quarantine).
