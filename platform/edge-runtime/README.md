# Edge Runtime (`edge-runtime`)

Lightweight embedded agent deployed on local factory gateways. Manages OT drivers (OPC-UA, MQTT, Modbus), local event buffering, and offline survival during WAN disconnects.
