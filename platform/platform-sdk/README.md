# Platform SDK Repository (`platform-sdk`)

Idiomatic SDK libraries (Go, TypeScript, Python) exposing telemetry interfaces, event publishing primitives, context propagation, and authentication wrappers.
