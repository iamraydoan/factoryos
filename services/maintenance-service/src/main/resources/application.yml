server:
  port: 8085

spring:
  application:
    name: maintenance-service
  datasource:
    url: jdbc:postgresql://localhost:5432/factoryos
    username: factoryos
    password: factoryos_password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

grpc:
  server:
    port: 9093

kafka:
  bootstrap-servers: localhost:9092
