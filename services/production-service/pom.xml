<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!--
      Inherited from ../pom.xml, NOT from spring-boot-starter-parent directly.
      Deliberately no <relativePath/>: that would skip the aggregator and let this
      service resolve a different Spring Boot than its siblings.
    -->
    <parent>
        <groupId>com.factoryos</groupId>
        <artifactId>factoryos-services-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>production-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <name>production-service</name>
    <description>FactoryOS Production Service</description>

    <dependencies>

        <!--
          spring-boot-starter-webmvc and spring-boot-starter-test are inherited from
          the parent; only what is specific to this service is declared here.
        -->

        <!-- Database -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!--
          gRPC server. This is Spring gRPC (org.springframework.grpc), which Spring Boot
          4.1.1 manages, replacing net.devh:grpc-spring-boot-starter. It brings grpc-netty
          and grpc-services, so no io.grpc dependency has to be pinned here.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-grpc-server</artifactId>
        </dependency>

        <!-- UUIDv7 entity identifiers -->
        <dependency>
            <groupId>com.github.f4b6a3</groupId>
            <artifactId>uuid-creator</artifactId>
            <version>6.1.1</version>
        </dependency>

        <!--
          Spring Boot 4 splits the test starters, so spring-boot-starter-test alone is
          not enough: each slice needs its own. The plain spring-boot-starter-test is
          inherited from the parent.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-grpc-server-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!--
              Protobuf + gRPC code generation. No <version> is needed: Spring Boot
              4.1.1's pluginManagement manages io.github.ascopes:protobuf-maven-plugin,
              and its BOMs manage protoc and protoc-gen-grpc-java. This replaces
              org.xolstice.maven.plugins:protobuf-maven-plugin, which is unmaintained.
            -->
            <plugin>
                <groupId>io.github.ascopes</groupId>
                <artifactId>protobuf-maven-plugin</artifactId>

                <configuration>
                    <sourceDirectories>
                        <sourceDirectory>
                            ${project.basedir}/../../api/contracts/production/v1
                        </sourceDirectory>
                    </sourceDirectories>

                    <plugins>
                        <plugin>
                            <groupId>io.grpc</groupId>
                            <artifactId>protoc-gen-grpc-java</artifactId>
                        </plugin>
                    </plugins>
                </configuration>

                <executions>
                    <execution>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>
    </build>

</project>
