# Architecture Repository (`architecture`)

Contains system C4 models, threat vectors, formal ADRs, and structural diagrams for the FactoryOS cloud and edge infrastructure.

* `c4-diagrams/`: Structurizr / PlantUML C4 Context, Container, Component models.
* `threat-models/`: Security threat models for multi-tenant cloud and zero-trust edge nodes.
